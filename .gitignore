# Created by https://www.gitignore.io/api/haskell

### Haskell ###
dist
cabal-dev
*.o
*.hi
*.chi
*.chs.h
*.dyn_o
*.dyn_hi
.hpc
.hsenv
.cabal-sandbox/
cabal.sandbox.config
*.prof
*.aux
*.hp
.stack-work/
dist-newstyle/
_cache/
_site/
# Local scratch — saved Scholar page, not for publishing
temp_vitae/

# Adversarial-review hand-off scratch files (kept local, never committed)
PR_*.md

# Stray per-run timer artifact that shows up across working dirs
timer.dat

# IRS SOI raw extract data — large source files, kept local (scripts in
# calcs/months-of-cash-at-scale/ document how to re-download and rebuild)
24eoextract990.zip
24eofinextractdoc.xlsx
calcs/data/
calcs/months-of-cash-at-scale/c3_months.csv.gz

# Original oversized hero export (kept; resized 1200x630 copy is the real one)
images/2026-07-07-months-of-cash-at-scale-hero.png.png

# Local build toolchain (Haskell Stack + TinyTeX) — large binaries, kept local
.tools/

### macOS ###
.DS_Store
calcs/__pycache__/
calcs/*.pyc
calcs/*.npy
research/**/__pycache__/
research/**/*.pyc

# Ephemeral locks and atomic-init staging for the tracked research workflow.
research/**/workflow/.transition.lock
research/**/.workflow-init.*.tmp
research/**/.workflow-recovery.*.tmp

# Regenerable H2+ fit detail and local Python/Psi4 scratch
calcs/h2plus-coulomb/predictions-*.csv
calcs/**/__pycache__/
psi.*.clean

# Correlation-in-hydrogenation local Psi4 output and scratch
/research/correlation-in-hydrogenation/_scratch/
/research/correlation-in-hydrogenation/psi4.out
/research/correlation-in-hydrogenation/run.log
/ijk.dat

# Regenerable pulse-independent-trajectory raw JSON. The publication bundle
# contains frozen copies; the compact joined analysis remains reviewable here.
downloads/pulse_ci_baseline.json
downloads/pulse_ci_convergence.json
downloads/pulse_ci_sweep.json
downloads/pulse_ci_exact_regimes.json
downloads/__pycache__/

# Obsolete one-off SIREN diagnostics (the publication data is tracked)
/downloads/conv_sweep.json
/downloads/sgd_equiv.json
/downloads/sgd_sweep.log

# Off-tonic experiment: regenerable score-page renders
research/off-tonic-recapitulation/tmp/

# DCML corpus clone (operator-only, reproducible from pinned commit)
research/off-tonic-recapitulation/data/sources/dcml/

# Source PDFs stay local unless a future artifact is explicitly force-added.
*.pdf
.codex-refs/
